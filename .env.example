# Secret key used for signing JWT authentication tokens (generate a long random hexadecimal string in production)
JWT_SECRET_KEY=

# Path to the SQLite database file
DB_PATH=

# FastAPI local server port
PORT=8000
